# OpenClaw Expert Skill

This is a specialized AI skill for **OpenClaw** — the self-hosted personal AI gateway.

## Overview

Unlike traditional documentation-heavy skills, this skill implements an **Introspection-based Methodology**. It guides the AI agent to use OpenClaw's built-in CLI tools to discover the "ground truth" of any local installation, ensuring accuracy regardless of version changes.

## Key Features

- **Self-Discovery**: Uses `openclaw config schema` to understand valid configuration patterns.
- **Environment Diagnostics**: Leverages `openclaw doctor` for troubleshooting and optimization.
- **Dynamic Capability Mapping**: Uses `openclaw nodes describe` and `openclaw models status` to map real-time system capabilities.

## Usage

This skill is intended to be loaded by an AI agent (like Gemini CLI or Claude Code) to provide expert-level assistance with OpenClaw management, configuration, and development.

---
**Generated by Andrew's Gemini CLI**
